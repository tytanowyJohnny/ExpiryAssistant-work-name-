<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColor">@color/fontColorPrimary</item>
    </style>

    <!-- Base application theme without ActionBar -->
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- System generated -->
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


    <!-- Font colors for different modules -->
    <style name="textInputLayout" parent="AppTheme">
        <item name="colorAccent">@color/fontColorTextInputLayout</item>
    </style>

    <style name="fontColorSingleRow" parent="AppTheme">
        <item name="android:textColor">@color/fontColorSingleRow</item>
    </style>

    <!-- Snackbar -->
    <style name="snackbarTheme" parent="AppTheme">
        <item name="android:textColor">@color/fontColorSnackbar</item>
    </style>



</resources>
